import assert from 'node:assert/strict';
import {before,beforeEach,afterEach,after,test} from 'node:test';
import {DatabaseSync} from 'node:sqlite';
import {readFile,mkdtemp,rm} from 'node:fs/promises';
import path from 'node:path';
import os from 'node:os';
import {pathToFileURL} from 'node:url';
import {banhoThankYouUrl,prepararCampanhaPdfBanho} from '../src/lib/pdf-casa-banho-campaign.mjs';
import {trackKitRegistration} from '../src/lib/kit-janelas-measurement.mjs';
let handler,dir,sql,env,calls;
const originalFetch=globalThis.fetch;
const data={email:'Pessoa@example.com',consent1:true,consent2:false,consentVersion:'pdf-casa-banho-2026-09-a',source:'pdf-casa-de-banho',eventId:'e2c4c975-2c92-4198-865f-cd5c1d32f5d8',company:''};
const cookie='gp_cookie_preferences='+encodeURIComponent(JSON.stringify({measurement:true,version:'2026-09-01-1',savedAt:new Date().toISOString()}));
before(async()=>{const {build}=await import(process.env.KIT_TEST_ESBUILD||'esbuild');dir=await mkdtemp(path.join(process.env.KIT_TEST_TMPDIR||os.tmpdir(),'pdf-banho-test-'));const outfile=path.join(dir,'api.mjs');await build({entryPoints:['functions/api/pdf-casa-de-banho.ts'],outfile,bundle:true,platform:'node',format:'esm',tsconfigRaw:{compilerOptions:{}}});handler=(await import(pathToFileURL(outfile))).onRequestPost;});
beforeEach(async()=>{sql=new DatabaseSync(':memory:');sql.exec(await readFile('migrations/pdf-casa-banho/0001_pdf_casa_banho.sql','utf8'));sql.exec(await readFile('migrations/consent-archive/0001_consent_evidence.sql','utf8'));const db={prepare(q){let args=[];return{bind(...a){args=a;return this},async first(){return sql.prepare(q).get(...args)||null},async run(){sql.prepare(q).run(...args);return{success:true}}}}};env={PDF_CASA_BANHO_DB:db,CONSENT_ARCHIVE_DB:db,CONSENT_ARCHIVE_KEY:'synthetic-archive-secret-for-local-tests-only',SESSION_SECRET:'local-test-only',SENDER_API_TOKEN:'synthetic-token',META_CAPI_ACCESS_TOKEN:'synthetic-meta'};calls=[];globalThis.fetch=async(url,init={})=>{calls.push({url:String(url),method:init.method,body:init.body?JSON.parse(init.body):null});if(init.method==='GET')return new Response('{}',{status:404});return Response.json(String(url).includes('graph.facebook.com')?{events_received:1}:{success:true});};});
afterEach(()=>{sql.close();globalThis.fetch=originalFetch;});after(async()=>{if(dir&&path.basename(dir).startsWith('pdf-banho-test-'))await rm(dir,{recursive:true,force:true});});
const submit=(changes={},headers={})=>handler({env,request:new Request('https://guiadoproprietario.pt/api/pdf-casa-de-banho',{method:'POST',headers:{'Content-Type':'application/json',Origin:'https://guiadoproprietario.pt','CF-Connecting-IP':'192.0.2.1',...headers},body:JSON.stringify({...data,...changes})})});
test('opt-in básico inclui Newsletter e dw87gX, envia PDF e CTA correto sem grupo comercial',async()=>{assert.equal((await submit()).status,200);const contact=calls.find(c=>c.url.endsWith('/subscribers')).body;assert.deepEqual(contact.groups,['eEvG4m','dw87gX']);assert.equal(contact.trigger_automation,false);assert.equal(contact.fields['{$CONSENT_PUBLICIDADE}'],undefined);const mail=calls.find(c=>c.url.endsWith('/message/send')).body;assert.match(mail.attachments['Guia-Banheira-Base-de-Duche-2026.pdf'],/guia-banheira-base-de-duche-2026.pdf$/);assert.match(mail.html,/go\/casa-banho-campanha\?source=pdf-casa-de-banho-email/);assert.doesNotMatch(mail.html,/Janelas|25%/);assert.equal(sql.prepare('SELECT count(*) n FROM inscricoes_meta').get().n,1);assert.equal(sql.prepare('SELECT estado_meta FROM inscricoes_meta').get().estado_meta,'sem_autorizacao');});
test('segunda caixa acrescenta apenas o grupo comercial autorizado',async()=>{await submit({consent2:true});assert.deepEqual(calls.find(c=>c.url.endsWith('/subscribers')).body.groups,['eEvG4m','dw87gX','egK8WG']);});
test('contacto existente recebe dw87gX sem substituir grupos e associações existentes',async()=>{const delegate=globalThis.fetch;globalThis.fetch=async(url,init={})=>{if(init.method==='GET')return Response.json({data:{subscriber_tags:[{id:'eEvG4m'},{id:'outro-kit'}]}});return delegate(url,init);};await submit();assert.deepEqual(calls.filter(c=>c.url.includes('/subscribers/groups/')).map(c=>c.url.split('/').at(-1)),['dw87gX']);assert.equal(calls.find(c=>c.method==='PATCH').body.groups,undefined);});
test('falha de associação a dw87gX nunca apresenta sucesso nem envia email',async()=>{const delegate=globalThis.fetch;globalThis.fetch=async(url,init={})=>{if(init.method==='GET')return Response.json({data:{subscriber_tags:[{id:'eEvG4m'}]}});if(String(url).endsWith('/groups/dw87gX'))return new Response('{}',{status:503});return delegate(url,init);};assert.equal((await submit()).status,502);assert.ok(!calls.some(c=>c.url.endsWith('/message/send')));});
test('repetição do pedido não reenvia PDF ou conversão; Pixel e CAPI usam ID comum',async()=>{const change={metaMeasurement:true,metaFbp:'fb.1.1788888000000.123456789'};const result=await(await submit(change,{Cookie:cookie})).json();await submit(change,{Cookie:cookie});assert.equal(calls.filter(c=>c.url.endsWith('/message/send')).length,1);const meta=calls.filter(c=>c.url.includes('graph.facebook.com'));assert.equal(meta.length,1);assert.equal(meta[0].body.data[0].event_name,'CompleteRegistration');assert.equal(meta[0].body.data[0].event_id,result.metaEventId);let pixel;trackKitRegistration({cookie},{fbq(...args){pixel=args}},result.metaEventId,'pdf-casa-de-banho');assert.equal(pixel[2].content_name,'pdf-casa-de-banho');assert.equal(pixel[3].eventID,result.metaEventId);});
test('consentimento PDF ou comercial não autoriza medição',async()=>{await submit({consent2:true,metaMeasurement:true});assert.ok(!calls.some(c=>c.url.includes('graph.facebook.com')));});
test('exige versão e consentimento próprios e nunca usa a base Janelas como alternativa',async()=>{assert.equal((await submit({consent1:false})).status,400);assert.equal((await submit({consentVersion:'kit-janelas-2026-09-b'})).status,400);env.KIT_JANELAS_DB=env.PDF_CASA_BANHO_DB;delete env.PDF_CASA_BANHO_DB;assert.equal((await submit()).status,503);assert.equal(calls.length,0);assert.throws(()=>sql.exec("INSERT INTO kit_events(source,event,status,occurred_at) VALUES('kit-trocar-janelas','x','x','x')"));});
test('páginas de agradecimento e links excluem email e mantêm a oferta do cliente',()=>{const url=banhoThankYouUrl('https://guiadoproprietario.pt/pdf-casa-de-banho/?utm_content=anuncio1&email=pessoa@example.com');assert.match(url,/pdf-casa-de-banho\/obrigado\//);assert.doesNotMatch(url,/email|pessoa/);assert.match(url,/utm_content=anuncio1/);const listeners={};const link={addEventListener(e,fn){listeners[e]=fn}};const doc={cookie:'',querySelectorAll(){return[link]}};prepararCampanhaPdfBanho(doc,{location:{href:url}});const tracker=new URL(link.href);assert.equal(tracker.pathname,'/go/casa-banho-campanha');assert.equal(tracker.searchParams.get('source'),'pdf-casa-de-banho-obrigado');assert.equal(tracker.searchParams.get('measurement_consent'),null);assert.equal(tracker.searchParams.get('utm_content'),'anuncio1');});
