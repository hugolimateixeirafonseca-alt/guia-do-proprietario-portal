name: Diagnose Pages Deployment Temp

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Diagnostic workflow disabled"
