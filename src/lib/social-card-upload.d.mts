export function isMultipartFormData(contentType:unknown):boolean;
export function detectRasterImageType(input:ArrayBuffer|Uint8Array):string;
export function isSupportedRasterImage(input:ArrayBuffer|Uint8Array,declaredType:unknown):boolean;
